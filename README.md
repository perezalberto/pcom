# pcom
project commands

```bash
pcom init #init a pcom.config.json
pcom run [command] #run a command
```

# pcom.config.json file struct

```json
{
    "commands": [
        {
            "name": "COMMAND_NAME",
            "command": ""
        }
    ]
}
```